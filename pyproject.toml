[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "vulnerable-notes-lab"
version = "0.1.0"
description = "Intentionally vulnerable notes application for OWASP Top 10 practice and AppSec portfolio writeups."
requires-python = ">=3.9"
dependencies = []
